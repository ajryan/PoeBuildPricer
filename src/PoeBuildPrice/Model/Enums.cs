namespace PoeBuildPrice.Model {
    public enum Rarity {
        Normal,
        Magic,
        Rare,
        Unique,
        Gem
    }

    public enum AlternateQuality {
        Anomolous,
        Divergent,
        Phantasmal
    }
}